{
    "name": "prashikshan-app-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build:frontend": "npm run build --prefix frontend",
        "deploy:frontend": "npm run build:frontend && firebase deploy --only hosting",
        "deploy:backend": "firebase deploy --only functions",
        "deploy": "npm run build:frontend && firebase deploy"
    }
}